import { withRouter } from 'react-router';

const About: React.FC = (): JSX.Element => {
    return (
        <p>
            About page working!
        </p>
    )
}

export default withRouter(About);